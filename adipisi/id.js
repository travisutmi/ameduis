// Assuming 'context' is your canvas 2D drawing context
const paddingX = context.canvas.clientLeft;
const paddingY = context.canvas.clientTop;

console.log(`Padding on X-axis: ${paddingX}px`);
console.log(`Padding on Y-axis: ${paddingY}px`);
